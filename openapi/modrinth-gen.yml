'!include': common.yml
inputSpec: https://docs.modrinth.com/openapi.yaml
packageName: modrinth-client
outputDir:  modrinth-client

parameterNameMappings:
  'id|slug': mod_id
  'id|username': user
  'id|number': id

nameMappings:
  '{version}-recommended': version_recommended
  '{version}-latest': version_latest
